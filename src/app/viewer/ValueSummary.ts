

export class ValueSummary {
    min:number;
    max:number;
    average:number;


    constructor(min:number, max:number, average:number) {
        this.min = min;
        this.max = max;
        this.average = average;
    }


}
